function createApp(name): set command = "cordova create " + name + " com.vpp." + name; system(command);| 

function compile(file, app): set command = "mono ~/v++/webtools/compiler.exe " + file; system(command);
